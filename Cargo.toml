[package]
name = "dream-path"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
license = "GPL-3.0-only"

[dependencies]
bstr = { version = "1.12", default-features = false, features = ["std"] }
